# Flutter — pass at run/build time:
#   flutter run --dart-define-from-file=.env
#   flutter build apk --dart-define=API_BASE_URL=https://unimarket-api.youngfuturetechnology.xyz
#
# Note: Api__PublicBaseUrl is the *backend* env var name, not Flutter's.
# Release APKs default to productionUrl in lib/core/config/api_config.dart.
#
# Copy to .env locally (gitignored). Never commit .env.

API_BASE_URL=https://unimarket-api.youngfuturetechnology.xyz
